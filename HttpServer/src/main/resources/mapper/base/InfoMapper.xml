<?xml version="1.0" encoding="UTF-8"?>
<!--
       Copyright 2010-2013 the original author or authors.

       Licensed under the Apache License, Version 2.0 (the "License");
       you may not use this file except in compliance with the License.
       You may obtain a copy of the License at

          http://www.apache.org/licenses/LICENSE-2.0

       Unless required by applicable law or agreed to in writing, software
       distributed under the License is distributed on an "AS IS" BASIS,
       WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
       See the License for the specific language governing permissions and
       limitations under the License.


CREATE TABLE `bt_info` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `title` varchar(255) NOT NULL,
  `description` TEXT DEFAULT NULL,
  `user_id` BIGINT(20) NOT NULL,
  `group_id` BIGINT(20) NOT NULL COMMENT '考虑到更换公司的信息',
  `status` tinyint(1) NOT NULL DEFAULT '1' COMMENT '1：未发布（新建）2：发布 3：取消 4：完成 5：结束',
  `previous_status` tinyint(1) NOT NULL DEFAULT '1' COMMENT '1：未发布（新建）2：发布 3：取消 4：完成 5：结束',
  `is_lock` tinyint(1) NOT NULL DEFAULT '1' COMMENT '1: 未锁定  2：锁定',
  `expire_time` timestamp NULL DEFAULT NULL,
  `create_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT 'CURRENT_TIMESTAMP',
  `update_time` timestamp NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP,
  `publish_time` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="com.baitaner.common.mapper.IInfoMapper">
  	<cache />
	<resultMap id="infoResultMapper" type="Info">
		<id property="id" column="id" />
		<result property="title" column="title" />
		<result property="description" column="description" />
		<result property="userId" column="user_id" />
		<result property="groupId" column="group_id" />
		<result property="status" column="status" />
		<result property="previousStatus" column="previous_status" />
		<result property="isLock" column="is_lock" />
		<result property="expireTime" column="expire_time" />
		<result property="createTime" column="create_time" />
		<result property="updateTime" column="update_time" />
		<result property="publishTime" column="publish_time" />
	</resultMap>
	
  	<insert id="insert" parameterType="Info" useGeneratedKeys="true" keyProperty="id">
  		INSERT INTO bt_info(
  			title
  			,description
  			,user_id
  			,group_id
  			,status
  			,previous_status
  			,is_lock
  			,expire_time
  			,create_time
  			,update_time
  			,publish_time
  		) values (
  			#{title}
  			,#{description}
  			,#{userId}
  			,#{groupId}
  			,#{status}
  			,#{previousStatus}
  			,#{isLock}
  			,#{expireTime}
  			,#{createTime}
  			,#{updateTime}
  			,#{publishTime}
  		)
  	</insert>
  	
  	<select id="findById" parameterType="long" resultMap="infoResultMapper" useCache="false">
		select
		*
		from
		bt_info where id = #{id}
	</select>

	<select id="findByUserIdAndStatus" resultMap="infoResultMapper" useCache="false">
		select
        *
		from
		bt_info where user_id = #{userId} and status=#{status} limit #{limit}
	</select>
	<select id="findByGroupIdAndStatus" resultMap="infoResultMapper" useCache="false">
		select
		*
		from
		bt_info where group_id = #{groupId} and status=#{status} limit #{limit}
	</select>
	<update id="update" parameterType="Info">
		UPDATE bt_info
		SET
		title=#{title}
		,description=#{description}
		,user_id=#{userId}
		,group_id=#{groupId}
		,status=#{status}
		,previous_status=#{previous_status}
		,is_lock=#{is_lock}
		,expire_time=#{expireTime}
		,create_time=#{createTime}
		,update_time=#{updateTime}
		,publish_time=#{publicTime}
		where id = #{id};
	</update>

</mapper>